name: 'insurance_lakehouse'
version: '1.0.0'
config-version: 2

profile: 'insurance_lakehouse'

model-paths: ["models"]
test-paths: ["tests"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  insurance_lakehouse:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
