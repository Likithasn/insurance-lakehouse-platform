# Power BI

Save your `.pbix` file here before committing (e.g.
`insurance_lakehouse_dashboard.pbix`).

Remember: GitHub cannot render `.pbix` files in the browser — clicking
it here only downloads it. Make sure the "Publish to web" link is set
in the main README so viewers can see the live, interactive report
without needing Power BI Desktop installed.
